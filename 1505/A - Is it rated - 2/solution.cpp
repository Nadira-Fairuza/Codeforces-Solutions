#include <bits/stdc++.h>
using namespace std;
 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    string s;
    while (getline(cin, s)) {
        cout << "NO" << endl;
        cout.flush(); // important for interactive problems
    }
 
    return 0;
}